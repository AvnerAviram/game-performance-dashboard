# Dependencies
node_modules/

# Test coverage
coverage/
.nyc_output/

# Playwright
playwright-report/
test-results/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
.temp/

# Build artifacts
dist/
build/
release/
release-v*.zip

# Environment (keep .env.example committed)
.env
.env.*
!.env.example

# Server auth credentials (never commit)
server/users.json

# Feedback tickets (internal state)
server/tickets.json

# Session files (server-side session store)
server/.sessions/

# IIS logs
logs/

# Build artifacts (regenerated by npm run build:data / npm install)
data/games.parquet
data/games_processed.json
public/duckdb/

# Cache & generated
cache/
*.backup*
*.bak*
*.broken

# Test screenshots (regenerated)
tests/screenshots/
tests/comparison/
tests/baseline-screenshots/
screenshots/
verification-screenshots/

# Data backups (keep games_master.json only)
data/*.backup*
data/*.cleaned.json

# CSV exports and data files (internal/proprietary)
*.csv

# E2E test screenshots and debug logs (generated during runs)
tests/e2e/*.png
tests/e2e/*.txt

# Python cache
__pycache__/
*.pyc

# Data pipeline scratch files (prefixed with _)
data/_*.json
data/_*.py
data/_*.html
data/_*.cjs
data/_backup_*/
data/_legacy/

# Extraction pipeline intermediates
data/extraction_results*.jsonl
data/extraction_checkpoint.json
data/extraction_sparse_games.txt
data/*_test_results.jsonl

# Data backups (large, regenerable)
data/game_data_master_backup_*.json

# Staging intermediates (used by pipeline scripts, not app)
data/staged_best_of_sources.json
data/staged_specs_external.json
data/thin_gt_extractions.json

# Downloaded spreadsheets
data/*.xlsx

# Husky (replaced by .hooks/)
.husky/
